name: Feature request
description: Propose an addition or change to tai-kit
labels: ["enhancement"]
body:
  - type: textarea
    id: description
    attributes:
      label: Description
      description: What should be added or changed, and what problem does it solve? Remember the leaf rule — tai-kit's only tai-* dependency is tai-contract, and optional backends stay behind extras.
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: tai-kit version
      description: The version you are using, if relevant.
      placeholder: "0.1.0"
    validations:
      required: false
